%table.table.mdl-data-table.fullwidth.ml-table-striped.ml-table-bordered.sequenceInputWidht{"template-pagination" => "custom/pager"}
  %tr
    - registration_columns = Registration.get_columns(@event).each do |column|
      %th
        = column[1]["label"]
    %th
  - objs = UserRegistration.user_registration_data(@event)
  - objs.each do |user_registred_data|
    %tr
      - registration_columns.each do |column_name|
        %td
          = user_registred_data[column_name[0]]